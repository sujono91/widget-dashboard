const HOST = 'http://localhost:4200/assets';

export const ENDPOINT = {
  CONFIDENTIALITY_DATA: `${HOST}/confidentiality_data.json`,
  CONFIDENTIALITY_LABEL: `${HOST}/confidentiality_labels.json`,
  DOCTYPE_DATA: `${HOST}/doctype_data.json`,
  DOCTYPE_LABEL: `${HOST}/doctype_labels.json`,
  LANGUAGE_DATA: `${HOST}/language_data.json`,
  LANGUAGE_LABEL: `${HOST}/language_labels.json`
};

export const NAME = {
  CONFIDENTIALITY: 'confidentiality',
  DOCTYPE: 'doctype',
  LANGUAGE: 'language'
};
